#!/bin/env python3
import os
import sys

sys.path.insert(1, '@python_dir@')

build_dir = os.environ.get('MESON_BUILD_ROOT')
if build_dir:
    sys.path.insert(1, build_dir)

import rencher
rencher.launch()
