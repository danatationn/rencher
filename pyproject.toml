[project]
name = "Rencher"
version = "1.2.1"
description = "A Ren'Py game manager, made with DDLC mods in mind"
requires-python = ">=3.12"
dependencies = [
    "pycairo==1.27.0; sys.platform == 'linux'",
    "pygobject==3.50.0; sys.platform == 'linux'",
    # apparently doesn't build on windows?
    "cx-freeze>=8.3.0; sys.platform == 'linux'",
    "rarfile>=4.2",
    "requests>=2.32.4",
    "thefuzz>=0.22.1",
    "watchdog>=6.0.0",
    "pygobject-stubs>=2.13.0",
    "sqlalchemy>=2.0.44",
    "pygobject>=3.50.0",
]


[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
py-modules = ["rencher"]

[tool.setuptools.packages.find]
where = ["."]
# so only modules get added
namespaces = false


[tool.pyright]
# pyright really love to complain about pygobject stubs not existing
reportMissingModuleSource = false

[tool.ruff]
line-length = 120

[tool.ruff.lint]
select = [
    "E",
    "F",
    "UP",
    "I",
]
extend-select = [
    "B",
    "COM",
]


[tool.ruff.format]
quote-style = "single"
indent-style = "space"

[dependency-groups]
dev = [
    "pygobject-stubs>=2.13.0",
]

[tool.ruff.lint.flake8-implicit-str-concat]
allow-multiline = false
